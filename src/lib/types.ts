export type App = {
  id: string;
  name: string;
  description: string;
  icon: string;
  downloadLink: string;
  category: string;
  tags: string[];
};

export type AppSettings = {
  adsensePublisherId: string;
};
